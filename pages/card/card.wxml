<!--card.wxml-->
<import src="../../components/index.wxml"/>

<view wx:for="{{cards}}" wx:for-index="idx" wx:for-item="item">
  <template is="components"
            data="{{type: item.type, data: item.data, styles: item.styles}}"
  />
</view>
